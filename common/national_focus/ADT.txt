focus_tree = {
	id = ADT_focus

	focus = { 
		id = ADT_embargo_focus
		icon = GFX_focus_ITA_corpo_volontari_della_liberta
		
		x = 0
		y = 2
		
		allow_branch = 
		{
			tag = ADT
		}

		cost = 1

		ai_will_do = {
			factor = 20
		}

		available = {
		}

		

		completion_reward = {
			send_embargo = SOV
			send_embargo = ITA 
			send_embargo = GER 
			send_embargo = HUN
			send_embargo = ROM
			send_embargo = BUL
			send_embargo = BRA 
			send_embargo = ENG 
			send_embargo = SAF 
			send_embargo = CAN 
			send_embargo = AST
			send_embargo = SPR
			send_embargo = FIN 
			send_embargo = MON 
			send_embargo = FRA 
		}

	}

	focus = { 
		id = JAP_chi_war
		icon = GFX_focus_ITA_corpo_volontari_della_liberta
		
		x = 0
		y = 4
		
		allow_branch = 
		{
			if = 
			{
				ENG = 
				{
					has_completed_focus = ENG_man_with_the_plan
				}
			}
			tag = ADT
		}

		cost = 1

		ai_will_do = {
			factor = 20
		}

		available = {
			date > 1937.7.7
		}

		

		completion_reward = {
			add_threat = 15 
		}

	}
}