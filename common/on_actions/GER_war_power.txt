on_actions = {
	on_war = {
        effect = {
			if = {
				limit = {
					tag = GER
					has_country_flag = GER_prioritize_economic_growth_flag
				}
				country_event = { id = germany.670 days = 1 }
			}
		}
	}
}
