Shared_AXIS_blitzkrieg_eco = 
{
	axis_blitzkrieg_timer = {
		icon = GFX_decision_category_ger_military_buildup

		visible = { 
            is_in_faction_with = GER
            has_global_flag = axis_blitzkrieg_eco
        }

		activation = {
			has_global_flag = axis_blitzkrieg_eco
		}
        
        available = { always = no }

		fire_only_once = yes
		days_mission_timeout = 1800  ## about 4 Years -> 1942 if completed after anschluss
        priority = 100


		timeout_effect = {
			ROOT = 
			{
				set_country_flag = axis_fake_collapse
			}
			if = 
			{
				limit = 
				{
					GER = 
					{
						has_country_flag = axis_fake_collapse
					}
				}
				set_global_flag = axis_financial_collapse
			}
		}
	}

    axis_blitzkrieg_france = {
		icon = GFX_decision_generic_GER_flag

		allowed = {
			tag = GER
		}

		available = {
                16 = { NOT = { is_controlled_by = FRA } }

        }
        cost = 0
        days_remove = 0
        fire_only_once = yes

        complete_effect = {
            add_days_mission_timeout = { mission = axis_blitzkrieg_timer days = 90 }
        }
	}

    axis_blitzkrieg_norway = {
		icon = GFX_decision_NOR_deploy_prep_dem_2

		allowed = {
			tag = GER
		}

		available = {
                144 = { NOT = { is_controlled_by = NOR } }

        }
        cost = 0
        days_remove = 0
        fire_only_once = yes

        complete_effect = {
            add_days_mission_timeout = { mission = axis_blitzkrieg_timer days = 120 }
        }
	}

    axis_blitzkrieg_africa = {
		icon = GFX_decision_generic_GER_flag

		allowed = {
			tag = GER
		}

		 available = {
        447 = { NOT = { is_controlled_by = ENG } }
        907 = { NOT = { is_controlled_by = ENG } }
        446 = { NOT = { is_controlled_by = ENG } }
        }
        cost = 0
        days_remove = 0
        fire_only_once = yes

        complete_effect = {
            add_days_mission_timeout = { mission = axis_blitzkrieg_timer days = 120 }
        }
	}

    axis_blitzkrieg_saf = {
		icon = GFX_decision_generic_GER_flag

		allowed = {
			tag = GER
		}

		available = {
            OR = {
                681 = { is_controlled_by = ITA }
                681 = { is_controlled_by = GER }
            }
        }
        cost = 0
        days_remove = 0
        fire_only_once = yes

        complete_effect = {
            add_days_mission_timeout = { mission = axis_blitzkrieg_timer days = 200 }
        }
	}

    axis_blitzkrieg_uk = {
		icon = GFX_decision_generic_GER_flag

		allowed = {
			tag = GER
		}

		available = {
                133 = { NOT = { is_controlled_by = ENG } }
                126 = { NOT = { is_controlled_by = ENG } }

        }
        cost = 0
        days_remove = 0
        fire_only_once = yes

        complete_effect = {
            add_days_mission_timeout = { mission = axis_blitzkrieg_timer days = 350 }
        }
	}

    axis_blitzkrieg_gib = {
		icon = GFX_decision_generic_GER_flag

		allowed = {
			tag = GER
		}

		available = {
                118 = { NOT = { is_controlled_by = ENG } }

        }
        cost = 0
        days_remove = 0
        fire_only_once = yes

        complete_effect = {
            add_days_mission_timeout = { mission = axis_blitzkrieg_timer days = 120 }
        }
	}

    axis_blitzkrieg_kiev = {
		icon = GFX_decision_SOV_socialism_in_one_country

		allowed = {
			tag = GER
		}

		available = {
                202 = { NOT = { is_controlled_by = SOV } }

        }
        cost = 0
        days_remove = 0
        fire_only_once = yes

        complete_effect = {
            add_days_mission_timeout = { mission = axis_blitzkrieg_timer days = 60 }
        }
	}

    axis_blitzkrieg_moscow = {
		icon = GFX_decision_SOV_socialism_in_one_country

		allowed = {
			tag = GER
		}

		available = {
                219 = { NOT = { is_controlled_by = SOV } }

        }
        cost = 0
        days_remove = 0
        fire_only_once = yes

        complete_effect = {
            add_days_mission_timeout = { mission = axis_blitzkrieg_timer days = 150 }
        }
	}

    axis_blitzkrieg_leningrad = {
		icon = GFX_decision_SOV_socialism_in_one_country

		allowed = {
			tag = GER
		}

		available = {
                219 = { NOT = { is_controlled_by = SOV } }

        }
        cost = 0
        days_remove = 0
        fire_only_once = yes

        complete_effect = {
            if = 
            {
                limit = { country_exists = FIN }
                add_days_mission_timeout = { mission = axis_blitzkrieg_timer days = 60 }
            }
            else = 
            {
                add_days_mission_timeout = { mission = axis_blitzkrieg_timer days = 150 }
            }
        }
	}

    axis_blitzkrieg_stalingrad = {
		icon = GFX_decision_SOV_socialism_in_one_country

		allowed = {
			tag = GER
		}

		available = {
                217 = { NOT = { is_controlled_by = SOV } }

        }
        cost = 0
        days_remove = 0
        fire_only_once = yes

        complete_effect = {
            add_days_mission_timeout = { mission = axis_blitzkrieg_timer days = 150 }
        }
	}

    axis_blitzkrieg_india = {
		icon = GFX_decision_generic_GER_flag

		allowed = {
			tag = GER
		}

		available = {
                439 = { is_controlled_by = GER }
        }

        visible = 
        {
            NOT = {has_global_flag = noAsia}
        }
        cost = 0
        days_remove = 0
        fire_only_once = yes

        complete_effect = {
            add_days_mission_timeout = { mission = axis_blitzkrieg_timer days = 200 }
        }
	}
}

Shared_AXIS_the_waiting_game = 
{
    axis_waiting_game_timer = {
		icon = GFX_decision_gre_paying_ifc_debt 

		visible = { 
            is_in_faction_with = GER
            has_global_flag = axis_the_waiting_game
        }

		activation = {
			has_global_flag = axis_the_waiting_game
		}
        
        available = { always = no }

		fire_only_once = yes
		days_mission_timeout = 1620  ## about 4 Years -> 1942 if completed after anschluss
        priority = 100


		timeout_effect = {

			set_global_flag = axis_economy_unbound_timer
			
		}
	}

     axis_integrate_france = {
		icon = GFX_decision_NOR_prepare_state_dem_1

		allowed = {
			tag = GER
		}

		available = {
                16 = { NOT = { is_controlled_by = FRA } }

        }
        cost = 0
        days_remove = 0
        fire_only_once = yes

        complete_effect = {
            add_days_mission_timeout = { mission = axis_waiting_game_timer days = -90 }
        }
	}

    axis_integrate_norway = {
		icon = GFX_decision_NOR_deploy_prep_dem_2

		allowed = {
			tag = GER
		}

		available = {
                144 = { NOT = { is_controlled_by = NOR } }

        }
        cost = 0
        days_remove = 0
        fire_only_once = yes

        complete_effect = {
            add_days_mission_timeout = { mission = axis_waiting_game_timer days = -100 }
        }
	}

    axis_integrate_africa = {
		icon = GFX_decision_NOR_prepare_state_dem_2

		allowed = {
			tag = GER
		}

		 available = {
        447 = { NOT = { is_controlled_by = ENG } }
        907 = { NOT = { is_controlled_by = ENG } }
        446 = { NOT = { is_controlled_by = ENG } }
        }
        cost = 0
        days_remove = 0
        fire_only_once = yes

        complete_effect = {
            add_days_mission_timeout = { mission = axis_waiting_game_timer days = -120 }
        }
	}

    axis_integrate_saf = {
		icon = GFX_decision_NOR_prepare_state_dem_3

		allowed = {
			tag = GER
		}

		available = {
            OR = {
                681 = { is_controlled_by = ITA }
                681 = { is_controlled_by = GER }
            }
        }
        cost = 0
        days_remove = 0
        fire_only_once = yes

        complete_effect = {
            add_days_mission_timeout = { mission = axis_waiting_game_timer days = -200 }
        }
	}

    axis_integrate_uk = {
		icon = GFX_decision_NOR_prepare_state_dem_3

		allowed = {
			tag = GER
		}

		available = {
                133 = { NOT = { is_controlled_by = ENG } }
                126 = { NOT = { is_controlled_by = ENG } }

        }
        cost = 0
        days_remove = 0
        fire_only_once = yes

        complete_effect = {
            add_days_mission_timeout = { mission = axis_waiting_game_timer days = -350 }
        }
	}

    axis_integrate_gib = {
		icon = GFX_decision_NOR_prepare_state_dem_2

		allowed = {
			tag = GER
		}

		available = {
                118 = { NOT = { is_controlled_by = ENG } }

        }
        cost = 0
        days_remove = 0
        fire_only_once = yes

        complete_effect = {
            add_days_mission_timeout = { mission = axis_waiting_game_timer days = -120 }
        }
	}

    axis_integrate_kiev = {
		icon = GFX_decision_NOR_prepare_state_dem_1

		allowed = {
			tag = GER
		}

		available = {
                202 = { NOT = { is_controlled_by = SOV } }

        }
        cost = 0
        days_remove = 0
        fire_only_once = yes

        complete_effect = {
            add_days_mission_timeout = { mission = axis_waiting_game_timer days = -60 }
        }
	}

    axis_integrate_moscow = {
		icon = GFX_decision_NOR_prepare_state_dem_2

		allowed = {
			tag = GER
		}

		available = {
                219 = { NOT = { is_controlled_by = SOV } }

        }
        cost = 0
        days_remove = 0
        fire_only_once = yes

        complete_effect = {
            add_days_mission_timeout = { mission = axis_waiting_game_timer days = -150 }
        }
	}

    axis_integrate_leningrad = {
		icon = GFX_decision_NOR_prepare_state_dem_2

		allowed = {
			tag = GER
		}

		available = {
                219 = { NOT = { is_controlled_by = SOV } }

        }
        cost = 0
        days_remove = 0
        fire_only_once = yes

        complete_effect = {
            if = 
            {
                limit = { country_exists = FIN }
                add_days_mission_timeout = { mission = axis_waiting_game_timer days = -60 }
            }
            else = 
            {
                add_days_mission_timeout = { mission = axis_waiting_game_timer days = -150 }
            }
        }
	}

    axis_integrate_stalingrad = {
		icon = GFX_decision_NOR_prepare_state_dem_2

		allowed = {
			tag = GER
		}

		available = {
                217 = { NOT = { is_controlled_by = SOV } }

        }
        cost = 0
        days_remove = 0
        fire_only_once = yes

        complete_effect = {
            add_days_mission_timeout = { mission = axis_waiting_game_timer days = -150 }
        }
	}

    axis_integrate_india = {
		icon = GFX_decision_NOR_prepare_state_dem_3

		allowed = {
			tag = GER
		}

		available = {
                439 = { is_controlled_by = GER }
        }

        visible = 
        {
            NOT = {has_global_flag = noAsia}
        }
        cost = 0
        days_remove = 0
        fire_only_once = yes

        complete_effect = {
            add_days_mission_timeout = { mission = axis_waiting_game_timer days = -200 }
        }
	}

}



Shared_axis_army_specializations = 
{
	AXIS_massmob_specialization = {
		icon = GFX_decision_SWE_set_army_budget

		visible = { 
            is_in_faction_with = GER
            has_global_flag = AXIS_high_command_flag
            NOT = {ROOT = {has_country_flag = specialization_chosen_flag}}
            NOT = {ROOT = {has_country_flag = preview_flag}}
            NOT = {ROOT = {has_country_flag = specialization_in_progress_flag}}
        }

        complete_effect = {
            ROOT = {
                set_country_flag = specialization_in_progress_flag
            }
        }
        
        available = { }

        priority = 100
        days_remove = 5


		remove_effect = {
			ROOT = 
            {
                set_country_flag = specialization_chosen_flag
            }
            ROOT = 
            {
                set_country_flag = massmob_specialization_flag
                clr_country_flag = spforces_specialization_flag
                clr_country_flag = tank_specialization_flag
                clr_country_flag = navy_specialization_flag
                clr_country_flag = air_specialization_flag
                clr_country_flag = preview_flag
                clr_country_flag = specialization_in_progress_flag
            }
		}
	}

    AXIS_special_forces_specialization = {
		icon = GFX_decision_SWI_guisan_speech

		visible = { 
            is_in_faction_with = GER
            has_global_flag = AXIS_high_command_flag
            NOT = {ROOT = {has_country_flag = specialization_chosen_flag}}
            NOT = {ROOT = {has_country_flag = preview_flag}}
            NOT = {ROOT = {has_country_flag = specialization_in_progress_flag}}
        }

        complete_effect = {
            ROOT = {
                set_country_flag = specialization_in_progress_flag
            }
        }
        
        available = { }

        priority = 100
        days_remove = 5


		remove_effect = {
			ROOT = 
            {
                set_country_flag = specialization_chosen_flag
            }
            ROOT = 
            {
                set_country_flag = spforces_specialization_flag
                clr_country_flag = massmob_specialization_flag
                clr_country_flag = tank_specialization_flag
                clr_country_flag = navy_specialization_flag
                clr_country_flag = air_specialization_flag
                clr_country_flag = preview_flag
                clr_country_flag = specialization_in_progress_flag
            }
		}
	}

    AXIS_armored_warfare_specialization = {
		icon = GFX_decision_generic_tank

		visible = { 
            is_in_faction_with = GER
            has_global_flag = AXIS_high_command_flag
            NOT = {ROOT = {has_country_flag = specialization_chosen_flag}}
            NOT = {ROOT = {has_country_flag = preview_flag}}
            NOT = {ROOT = {has_country_flag = specialization_in_progress_flag}}
        }
        
        available = { }

        priority = 100
        days_remove = 5

        complete_effect = {
            ROOT = {
                set_country_flag = specialization_in_progress_flag
            }
        }

		remove_effect = {
			ROOT = 
            {
                set_country_flag = specialization_chosen_flag
            }
            ROOT = 
            {
                set_country_flag = tank_specialization_flag
                clr_country_flag = massmob_specialization_flag
                clr_country_flag = spforces_specialization_flag
                clr_country_flag = navy_specialization_flag
                clr_country_flag = air_specialization_flag
                clr_country_flag = preview_flag
                clr_country_flag = specialization_in_progress_flag
            }
		}
	}

    AXIS_navy_specialization = {
		icon = GFX_decision_SWE_set_navy_budget

		visible = { 
            is_in_faction_with = GER
            has_global_flag = AXIS_high_command_flag
            NOT = {ROOT = {has_country_flag = specialization_chosen_flag}}
            NOT = {ROOT = {has_country_flag = preview_flag}}
            NOT = {ROOT = {has_country_flag = specialization_in_progress_flag}}
        }
        
        available = { }

        priority = 100
        days_remove = 5

        complete_effect = {
            ROOT = {
                set_country_flag = specialization_in_progress_flag
            }
        }


		remove_effect = {
			ROOT = 
            {
                set_country_flag = specialization_chosen_flag
            }
            ROOT = 
            {
                set_country_flag = navy_specialization_flag
                clr_country_flag = massmob_specialization_flag
                clr_country_flag = spforces_specialization_flag
                clr_country_flag = tank_specialization_flag
                clr_country_flag = air_specialization_flag
                clr_country_flag = preview_flag
                clr_country_flag = specialization_in_progress_flag
            }
		}
	}

    AXIS_air_specialization = {
		icon = GFX_decision_generic_merge_plant_aircraft

		visible = { 
            is_in_faction_with = GER
            has_global_flag = AXIS_high_command_flag
            NOT = {ROOT = {has_country_flag = specialization_chosen_flag}}
            NOT = {ROOT = {has_country_flag = preview_flag}}
            NOT = {ROOT = {has_country_flag = specialization_in_progress_flag}}
        }
        
        available = { }

        priority = 100
        days_remove = 5

        complete_effect = {
            ROOT = {
                set_country_flag = specialization_in_progress_flag
            }
        }


		remove_effect = {
			ROOT = 
            {
                set_country_flag = specialization_chosen_flag
            }
            ROOT = 
            {
                set_country_flag = air_specialization_flag
                clr_country_flag = massmob_specialization_flag
                clr_country_flag = spforces_specialization_flag
                clr_country_flag = tank_specialization_flag
                clr_country_flag = navy_specialization_flag
                clr_country_flag = preview_flag
                clr_country_flag = specialization_in_progress_flag
            }
		}
    }
    
    ####PREVIEW

    AXIS_preview_specializations = {
		icon = GFX_decision_generic_decision

		visible = { 
            is_in_faction_with = GER
            has_global_flag = AXIS_high_command_flag
            NOT = {ROOT = {has_country_flag = specialization_chosen_flag}}
            NOT = {has_country_flag = preview_flag}
            NOT = {ROOT = {has_country_flag = specialization_in_progress_flag}}
        }
        
        available = { }

        priority = 100


		remove_effect = {
			ROOT = 
            {
                set_country_flag = preview_flag
            }
		}
    }


    ### PREVIEW - BRANCHES 

    AXIS_massmob_specialization_preview = {
		icon = GFX_decision_SWE_set_army_budget

		visible = { 
            is_in_faction_with = GER
            has_global_flag = AXIS_high_command_flag
            NOT = {ROOT = {has_country_flag = specialization_chosen_flag}}
            ROOT = {has_country_flag = preview_flag}
        }
        
        available = { }

        priority = 100


		remove_effect = {
			ROOT = 
            {
                set_country_flag = massmob_specialization_flag
                clr_country_flag = spforces_specialization_flag
                clr_country_flag = tank_specialization_flag
                clr_country_flag = navy_specialization_flag
                clr_country_flag = air_specialization_flag
                mark_focus_tree_layout_dirty = yes
            }
		}
	}

    AXIS_special_forces_specialization_preview = {
		icon = GFX_decision_SWI_guisan_speech

		visible = { 
            is_in_faction_with = GER
            has_global_flag = AXIS_high_command_flag
            NOT = {ROOT = {has_country_flag = specialization_chosen_flag}}
            ROOT = {has_country_flag = preview_flag}
        }
        
        available = { }

        priority = 100


		remove_effect = {
			ROOT = 
            {
                clr_country_flag = massmob_specialization_flag
                set_country_flag = spforces_specialization_flag
                clr_country_flag = tank_specialization_flag
                clr_country_flag = navy_specialization_flag
                clr_country_flag = air_specialization_flag
                mark_focus_tree_layout_dirty = yes
            }
		}
	}

    AXIS_armored_warfare_specialization_preview = {
		icon = GFX_decision_generic_tank

		visible = { 
            is_in_faction_with = GER
            has_global_flag = AXIS_high_command_flag
            NOT = {ROOT = {has_country_flag = specialization_chosen_flag}}
            ROOT = {has_country_flag = preview_flag}
        }
        
        available = { }

        priority = 100


		remove_effect = {
			ROOT = 
            {
                clr_country_flag = massmob_specialization_flag
                clr_country_flag = spforces_specialization_flag
                set_country_flag = tank_specialization_flag
                clr_country_flag = navy_specialization_flag
                clr_country_flag = air_specialization_flag
                mark_focus_tree_layout_dirty = yes
            }
		}
	}

    AXIS_navy_specialization_preview = {
		icon = GFX_decision_SWE_set_navy_budget

		visible = { 
            is_in_faction_with = GER
            has_global_flag = AXIS_high_command_flag
            NOT = {ROOT = {has_country_flag = specialization_chosen_flag}}
            ROOT = {has_country_flag = preview_flag}
        }
        
        available = { }

        priority = 100


		remove_effect = {
			ROOT = 
            {
                clr_country_flag = massmob_specialization_flag
                clr_country_flag = spforces_specialization_flag
                clr_country_flag = tank_specialization_flag
                set_country_flag = navy_specialization_flag
                clr_country_flag = air_specialization_flag
                mark_focus_tree_layout_dirty = yes
            }
		}
	}

    AXIS_air_specialization_preview = {
		icon = GFX_decision_generic_merge_plant_aircraft

		visible = { 
            is_in_faction_with = GER
            has_global_flag = AXIS_high_command_flag
            NOT = {ROOT = {has_country_flag = specialization_chosen_flag}}
            ROOT = {has_country_flag = preview_flag}
        }
        
        available = { }

        priority = 100


		remove_effect = {
			ROOT = 
            {
                clr_country_flag = massmob_specialization_flag
                clr_country_flag = spforces_specialization_flag
                clr_country_flag = tank_specialization_flag
                clr_country_flag = navy_specialization_flag
                set_country_flag = air_specialization_flag
                mark_focus_tree_layout_dirty = yes
            }
		}
    }


    #########

    AXIS_end_preview = {
		icon = GFX_decision_generic_decision

		visible = { 
            is_in_faction_with = GER
            has_global_flag = AXIS_high_command_flag
            NOT = {ROOT = {has_country_flag = specialization_chosen_flag}}
            ROOT = {has_country_flag = preview_flag}
        }
        
        available = { }

        priority = 100


		remove_effect = {
			ROOT = 
            {
                clr_country_flag = massmob_specialization_flag
                clr_country_flag = spforces_specialization_flag
                clr_country_flag = tank_specialization_flag
                clr_country_flag = navy_specialization_flag
                clr_country_flag = air_specialization_flag
                clr_country_flag = preview_flag
                mark_focus_tree_layout_dirty = yes
            }
		}
    }
}





