on_actions = {
    on_weekly = {
        effect = {
            every_country = {
                if = {
                    limit = {
                        is_ai = no
                        OR = {
                            is_in_faction_with = GER
                            is_in_faction_with = ENG
                        }
                    }
                    set_country_flag = accessible_shared_tree
                }
                else = {
                    clr_country_flag = accessible_shared_tree
                }
            }
        }
    }
}