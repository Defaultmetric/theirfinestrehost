Shared_ALLIES_blitzkrieg_eco = 
{
	blitzkrieg_timer = {
		icon = GFX_decision_generic_tank

		visible = { 
            is_in_faction_with = ENG
            has_global_flag = allies_blitzkrieg_eco
        }

		activation = {
			has_global_flag = allies_blitzkrieg_eco
		}
        
        available = { always = no }

		fire_only_once = yes
		days_mission_timeout = 1800  ## about 4 Years -> 1942 if completed after anschluss
        priority = 100


		timeout_effect = {
			ROOT = 
			{
				set_country_flag = allies_fake_collapse
			}
			if = 
			{
				limit = 
				{
					ENG = 
					{
						has_country_flag = allies_fake_collapse
					}
				}
				set_global_flag = allies_financial_collapse
			}
		}
	}

    blitzkrieg_france = {
		icon = GFX_decision_generic_GER_flag

		allowed = {
			tag = ENG
		}

		available = {
                16 = { NOT = { is_controlled_by = GER } }
                date > 1941.1.1
        }
        cost = 0
        days_remove = 0
        fire_only_once = yes

        complete_effect = {
            add_days_mission_timeout = { mission = blitzkrieg_timer days = 200 }
        }
	}

    blitzkrieg_norway = {
		icon = GFX_decision_NOR_deploy_prep_dem_2

		allowed = {
			tag = ENG
		}

		available = {
                110 = { NOT = { is_controlled_by = GER } }
                920 = { NOT = { is_controlled_by = GER } }
                date > 1941.1.1
        }
        cost = 0
        days_remove = 0
        fire_only_once = yes

        complete_effect = {
            add_days_mission_timeout = { mission = blitzkrieg_timer days = 90 }
        }
	}

    blitzkrieg_romania = {
		icon = GFX_decision_ROM_claim_yugo_1

		allowed = {
			tag = ENG
		}

		available = {
                46 = { is_controlled_by = ENG }
                79 = { is_controlled_by = ENG }
        }
        cost = 0
        days_remove = 0
        fire_only_once = yes

        complete_effect = {
            add_days_mission_timeout = { mission = blitzkrieg_timer days = 200 }
        }
	}

    blitzkrieg_spain = {
		icon = GFX_decision_spr_garrison_control_total

		allowed = {
			tag = ENG
		}

		available = {
                41 = { is_controlled_by = ENG }

        }
        cost = 0
        days_remove = 0
        fire_only_once = yes

        complete_effect = {
            add_days_mission_timeout = { mission = blitzkrieg_timer days = 250 }
        }
	}

    blitzkrieg_germany = {
		icon = GFX_decision_generic_GER_flag

		allowed = {
			tag = ENG
		}

		available = {
                64 = { NOT = { is_controlled_by = GER } }

        }
        cost = 0
        days_remove = 0
        fire_only_once = yes

        complete_effect = {
            add_days_mission_timeout = { mission = blitzkrieg_timer days = 350 }
        }
	}

    blitzkrieg_bulgaria = {
		icon = GFX_decision_bul_bolster_zveno

		allowed = {
			tag = ENG
		}

		available = {
            48 = { is_controlled_by = ENG } 

        }
        cost = 0
        days_remove = 0
        fire_only_once = yes

        complete_effect = {
            add_days_mission_timeout = { mission = blitzkrieg_timer days = 150 }
        }
	}

    blitzkrieg_africa = {
		icon = GFX_decision_ITA_claim_yugo_1

		allowed = {
			tag = ENG
		}

		 available = {
            448 = { NOT = { is_controlled_by = ITA } }
            450 = { NOT = { is_controlled_by = ITA } }
            451 = { NOT = { is_controlled_by = ITA } }
            449 = { NOT = { is_controlled_by = ITA } }
        }
        cost = 0
        days_remove = 0
        fire_only_once = yes

        complete_effect = {
            add_days_mission_timeout = { mission = blitzkrieg_timer days = 150 }
        }
	}

    blitzkrieg_sardegna = {
		icon = GFX_decision_ITA_claim_yugo_1

		allowed = {
			tag = ENG
		}

		available = {
                114 = { NOT = { is_controlled_by = ITA } }

        }
        cost = 0
        days_remove = 0
        fire_only_once = yes

        complete_effect = {
            add_days_mission_timeout = { mission = blitzkrieg_timer days = 90 }
        }
	}

    blitzkrieg_sicilia = {
		icon = GFX_decision_ITA_claim_yugo_1

		allowed = {
			tag = ENG
		}

		available = {
                115 = { NOT = { is_controlled_by = ITA } }

        }
        cost = 0
        days_remove = 0
        fire_only_once = yes

        complete_effect = {
          
                add_days_mission_timeout = { mission = blitzkrieg_timer days = 120 }

        
        }
	}

    blitzkrieg_rome = {
		icon = GFX_decision_ITA_claim_yugo_1

		allowed = {
			tag = ENG
		}

		available = {
                2 = { NOT = { is_controlled_by = ITA } }

        }
        cost = 0
        days_remove = 0
        fire_only_once = yes

        complete_effect = {
            add_days_mission_timeout = { mission = blitzkrieg_timer days = 150 }
        }
	}

    blitzkrieg_albania = {
		icon = GFX_decision_ITA_claim_yugo_1

		allowed = {
			tag = ENG
		}

		available = {

            44 = { NOT = { is_controlled_by = ITA } }
            date > 1940.1.1
        }

        cost = 0
        days_remove = 0
        fire_only_once = yes

        complete_effect = {
            add_days_mission_timeout = { mission = blitzkrieg_timer days = 60 }
        }
	}
}

Shared_ALLIES_the_waiting_game = 
{
    waiting_game_timer = {
		icon = GFX_decision_gre_paying_ifc_debt 

		visible = { 
            is_in_faction_with = ENG
            has_global_flag = allies_the_waiting_game
        }

		activation = {
			has_global_flag = allies_the_waiting_game
		}
        
        available = { always = no }

		fire_only_once = yes
		days_mission_timeout = 1620  ## about 4 Years -> 1942 if completed after anschluss
        priority = 100


		timeout_effect = {

			set_global_flag = allies_economy_unbound_timer
			
		}
	}

    integrate_france = {
	icon = GFX_decision_generic_GER_flag

	allowed = {
		tag = ENG
	}

	available = {
		16 = { NOT = { is_controlled_by = GER } }
		date > 1941.1.1
	}
	cost = 0
	days_remove = 0
	fire_only_once = yes

	complete_effect = {
		add_days_mission_timeout = { mission = waiting_game_timer days = -200 }
	}
}

integrate_norway = {
	icon = GFX_decision_NOR_deploy_prep_dem_2

	allowed = {
		tag = ENG
	}

	available = {
		110 = { NOT = { is_controlled_by = GER } }
		920 = { NOT = { is_controlled_by = GER } }
		date > 1941.1.1
	}
	cost = 0
	days_remove = 0
	fire_only_once = yes

	complete_effect = {
		add_days_mission_timeout = { mission = waiting_game_timer days = -90 }
	}
}

integrate_romania = {
	icon = GFX_decision_ROM_claim_yugo_1

	allowed = {
		tag = ENG
	}

	available = {
		46 = { is_controlled_by = ENG }
		79 = { is_controlled_by = ENG }
	}
	cost = 0
	days_remove = 0
	fire_only_once = yes

	complete_effect = {
		add_days_mission_timeout = { mission = waiting_game_timer days = -200 }
	}
}

integrate_spain = {
	icon = GFX_decision_spr_garrison_control_total

	allowed = {
		tag = ENG
	}

	available = {
		41 = { is_controlled_by = ENG }
	}
	cost = 0
	days_remove = 0
	fire_only_once = yes

	complete_effect = {
		add_days_mission_timeout = { mission = waiting_game_timer days = -250 }
	}
}

integrate_germany = {
	icon = GFX_decision_generic_GER_flag

	allowed = {
		tag = ENG
	}

	available = {
		64 = { NOT = { is_controlled_by = GER } }
	}
	cost = 0
	days_remove = 0
	fire_only_once = yes

	complete_effect = {
		add_days_mission_timeout = { mission = waiting_game_timer days = -350 }
	}
}

integrate_bulgaria = {
	icon = GFX_decision_bul_bolster_zveno

	allowed = {
		tag = ENG
	}

	available = {
		48 = { is_controlled_by = ENG }
	}
	cost = 0
	days_remove = 0
	fire_only_once = yes

	complete_effect = {
		add_days_mission_timeout = { mission = waiting_game_timer days = -150 }
	}
}

integrate_africa = {
	icon = GFX_decision_ITA_claim_yugo_1

	allowed = {
		tag = ENG
	}

	available = {
		448 = { NOT = { is_controlled_by = ITA } }
		450 = { NOT = { is_controlled_by = ITA } }
		451 = { NOT = { is_controlled_by = ITA } }
		449 = { NOT = { is_controlled_by = ITA } }
	}
	cost = 0
	days_remove = 0
	fire_only_once = yes

	complete_effect = {
		add_days_mission_timeout = { mission = waiting_game_timer days = -150 }
	}
}

integrate_sardegna = {
	icon = GFX_decision_ITA_claim_yugo_1

	allowed = {
		tag = ENG
	}

	available = {
		114 = { NOT = { is_controlled_by = ITA } }
	}
	cost = 0
	days_remove = 0
	fire_only_once = yes

	complete_effect = {
		add_days_mission_timeout = { mission = waiting_game_timer days = -90 }
	}
}

integrate_sicilia = {
	icon = GFX_decision_ITA_claim_yugo_1

	allowed = {
		tag = ENG
	}

	available = {
		115 = { NOT = { is_controlled_by = ITA } }
	}
	cost = 0
	days_remove = 0
	fire_only_once = yes

	complete_effect = {
		add_days_mission_timeout = { mission = waiting_game_timer days = -120 }
	}
}

integrate_rome = {
	icon = GFX_decision_ITA_claim_yugo_1

	allowed = {
		tag = ENG
	}

	available = {
		2 = { NOT = { is_controlled_by = ITA } }
	}
	cost = 0
	days_remove = 0
	fire_only_once = yes

	complete_effect = {
		add_days_mission_timeout = { mission = waiting_game_timer days = -150 }
	}
}

integrate_albania = {
	icon = GFX_decision_ITA_claim_yugo_1

	allowed = {
		tag = ENG
	}

	available = {
		44 = { NOT = { is_controlled_by = ITA } }
		date > 1940.1.1
	}

	cost = 0
	days_remove = 0
	fire_only_once = yes

	complete_effect = {
		add_days_mission_timeout = { mission = waiting_game_timer days = -60 }
	}
}

}

Shared_allies_army_specializations = 
{
	allies_massmob_specialization = {
		icon = GFX_decision_SWE_set_army_budget

		visible = { 
            is_in_faction_with = ENG
            has_global_flag = allies_high_command_flag
            OR = {
                tag = AST
                tag = ENG
                tag = CAN
                tag = SAF
                tag = FRA
                tag = BRA
            }
            NOT = {ROOT = {has_country_flag = specialization_chosen_flag}}
            NOT = {ROOT = {has_country_flag = preview_flag}}
            NOT = {ROOT = {has_country_flag = specialization_in_progress_flag}}
        }

        complete_effect = {
            ROOT = {
                set_country_flag = specialization_in_progress_flag
            }
        }
        
        available = { }

        priority = 100
        days_remove = 5


		remove_effect = {
			ROOT = 
            {
                set_country_flag = specialization_chosen_flag
            }
            ROOT = 
            {
                set_country_flag = massmob_specialization_flag
                clr_country_flag = spforces_specialization_flag
                clr_country_flag = tank_specialization_flag
                clr_country_flag = navy_specialization_flag
                clr_country_flag = air_specialization_flag
                clr_country_flag = preview_flag
                clr_country_flag = specialization_in_progress_flag
            }
		}
	}

    allies_special_forces_specialization = {
		icon = GFX_decision_SWI_guisan_speech

		visible = { 
            is_in_faction_with = ENG
            has_global_flag = allies_high_command_flag
            OR = {
                tag = AST
                tag = ENG
                tag = CAN
                tag = SAF
                tag = FRA
                tag = BRA
            }
            NOT = {ROOT = {has_country_flag = specialization_chosen_flag}}
            NOT = {ROOT = {has_country_flag = preview_flag}}
            NOT = {ROOT = {has_country_flag = specialization_in_progress_flag}}
        }

        complete_effect = {
            ROOT = {
                set_country_flag = specialization_in_progress_flag
            }
        }
        
        available = { }

        priority = 100
        days_remove = 5


		remove_effect = {
			ROOT = 
            {
                set_country_flag = specialization_chosen_flag
            }
            ROOT = 
            {
                set_country_flag = spforces_specialization_flag
                clr_country_flag = massmob_specialization_flag
                clr_country_flag = tank_specialization_flag
                clr_country_flag = navy_specialization_flag
                clr_country_flag = air_specialization_flag
                clr_country_flag = preview_flag
                clr_country_flag = specialization_in_progress_flag
            }
		}
	}

    allies_armored_warfare_specialization = {
		icon = GFX_decision_generic_tank

		visible = { 
            is_in_faction_with = ENG
            has_global_flag = allies_high_command_flag
            OR = {
                tag = AST
                tag = ENG
                tag = CAN
                tag = SAF
                tag = FRA
                tag = BRA
            }
            NOT = {ROOT = {has_country_flag = specialization_chosen_flag}}
            NOT = {ROOT = {has_country_flag = preview_flag}}
            NOT = {ROOT = {has_country_flag = specialization_in_progress_flag}}
        }
        
        available = { }

        priority = 100
        days_remove = 5

        complete_effect = {
            ROOT = {
                set_country_flag = specialization_in_progress_flag
            }
        }

		remove_effect = {
			ROOT = 
            {
                set_country_flag = specialization_chosen_flag
            }
            ROOT = 
            {
                set_country_flag = tank_specialization_flag
                clr_country_flag = massmob_specialization_flag
                clr_country_flag = spforces_specialization_flag
                clr_country_flag = navy_specialization_flag
                clr_country_flag = air_specialization_flag
                clr_country_flag = preview_flag
                clr_country_flag = specialization_in_progress_flag
            }
		}
	}

    allies_navy_specialization = {
		icon = GFX_decision_SWE_set_navy_budget

		visible = { 
            is_in_faction_with = ENG
            has_global_flag = allies_high_command_flag
            OR = {
                tag = AST
                tag = ENG
                tag = CAN
                tag = SAF
                tag = FRA
                tag = BRA
            }
            NOT = {ROOT = {has_country_flag = specialization_chosen_flag}}
            NOT = {ROOT = {has_country_flag = preview_flag}}
            NOT = {ROOT = {has_country_flag = specialization_in_progress_flag}}
        }
        
        available = { }

        priority = 100
        days_remove = 5

        complete_effect = {
            ROOT = {
                set_country_flag = specialization_in_progress_flag
            }
        }


		remove_effect = {
			ROOT = 
            {
                set_country_flag = specialization_chosen_flag
            }
            ROOT = 
            {
                set_country_flag = navy_specialization_flag
                clr_country_flag = massmob_specialization_flag
                clr_country_flag = spforces_specialization_flag
                clr_country_flag = tank_specialization_flag
                clr_country_flag = air_specialization_flag
                clr_country_flag = preview_flag
                clr_country_flag = specialization_in_progress_flag
            }
		}
	}

    allies_air_specialization = {
		icon = GFX_decision_generic_merge_plant_aircraft

		visible = { 
            is_in_faction_with = ENG
            has_global_flag = allies_high_command_flag
            OR = {
                tag = AST
                tag = ENG
                tag = CAN
                tag = SAF
                tag = FRA
                tag = BRA
            }
            NOT = {ROOT = {has_country_flag = specialization_chosen_flag}}
            NOT = {ROOT = {has_country_flag = preview_flag}}
            NOT = {ROOT = {has_country_flag = specialization_in_progress_flag}}
        }
        
        available = { }

        priority = 100
        days_remove = 5

        complete_effect = {
            ROOT = {
                set_country_flag = specialization_in_progress_flag
            }
        }


		remove_effect = {
			ROOT = 
            {
                set_country_flag = specialization_chosen_flag
            }
            ROOT = 
            {
                set_country_flag = air_specialization_flag
                clr_country_flag = massmob_specialization_flag
                clr_country_flag = spforces_specialization_flag
                clr_country_flag = tank_specialization_flag
                clr_country_flag = navy_specialization_flag
                clr_country_flag = preview_flag
                clr_country_flag = specialization_in_progress_flag
            }
		}
    }
    
    ####PREVIEW

    allies_preview_specializations = {
		icon = GFX_decision_generic_decision

		visible = { 
            is_in_faction_with = ENG
            has_global_flag = allies_high_command_flag
            OR = {
                tag = AST
                tag = ENG
                tag = CAN
                tag = SAF
                tag = FRA
                tag = BRA
            }
            NOT = {ROOT = {has_country_flag = specialization_chosen_flag}}
            NOT = {has_country_flag = preview_flag}
            NOT = {ROOT = {has_country_flag = specialization_in_progress_flag}}
        }
        
        available = { }

        priority = 100


		remove_effect = {
			ROOT = 
            {
                set_country_flag = preview_flag
            }
		}
    }


    ### PREVIEW - BRANCHES 

    allies_massmob_specialization_preview = {
		icon = GFX_decision_SWE_set_army_budget

		visible = { 
            is_in_faction_with = ENG
            has_global_flag = allies_high_command_flag
            OR = {
                tag = AST
                tag = ENG
                tag = CAN
                tag = SAF
                tag = FRA
                tag = BRA
            }
            NOT = {ROOT = {has_country_flag = specialization_chosen_flag}}
            ROOT = {has_country_flag = preview_flag}
        }
        
        available = { }

        priority = 100


		remove_effect = {
			ROOT = 
            {
                set_country_flag = massmob_specialization_flag
                clr_country_flag = spforces_specialization_flag
                clr_country_flag = tank_specialization_flag
                clr_country_flag = navy_specialization_flag
                clr_country_flag = air_specialization_flag
                mark_focus_tree_layout_dirty = yes
            }
		}
	}

    allies_special_forces_specialization_preview = {
		icon = GFX_decision_SWI_guisan_speech

		visible = { 
            is_in_faction_with = ENG
            has_global_flag = allies_high_command_flag
            OR = {
                tag = AST
                tag = ENG
                tag = CAN
                tag = SAF
                tag = FRA
                tag = BRA
            }
            NOT = {ROOT = {has_country_flag = specialization_chosen_flag}}
            ROOT = {has_country_flag = preview_flag}
        }
        
        available = { }

        priority = 100


		remove_effect = {
			ROOT = 
            {
                clr_country_flag = massmob_specialization_flag
                set_country_flag = spforces_specialization_flag
                clr_country_flag = tank_specialization_flag
                clr_country_flag = navy_specialization_flag
                clr_country_flag = air_specialization_flag
                mark_focus_tree_layout_dirty = yes
            }
		}
	}

    allies_armored_warfare_specialization_preview = {
		icon = GFX_decision_generic_tank

		visible = { 
            is_in_faction_with = ENG
            has_global_flag = allies_high_command_flag
            OR = {
                tag = AST
                tag = ENG
                tag = CAN
                tag = SAF
                tag = FRA
                tag = BRA
            }
            NOT = {ROOT = {has_country_flag = specialization_chosen_flag}}
            ROOT = {has_country_flag = preview_flag}
        }
        
        available = { }

        priority = 100


		remove_effect = {
			ROOT = 
            {
                clr_country_flag = massmob_specialization_flag
                clr_country_flag = spforces_specialization_flag
                set_country_flag = tank_specialization_flag
                clr_country_flag = navy_specialization_flag
                clr_country_flag = air_specialization_flag
                mark_focus_tree_layout_dirty = yes
            }
		}
	}

    allies_navy_specialization_preview = {
		icon = GFX_decision_SWE_set_navy_budget

		visible = { 
            is_in_faction_with = ENG
            has_global_flag = allies_high_command_flag
            OR = {
                tag = AST
                tag = ENG
                tag = CAN
                tag = SAF
                tag = FRA
                tag = BRA
            }
            NOT = {ROOT = {has_country_flag = specialization_chosen_flag}}
            ROOT = {has_country_flag = preview_flag}
        }
        
        available = { }

        priority = 100


		remove_effect = {
			ROOT = 
            {
                clr_country_flag = massmob_specialization_flag
                clr_country_flag = spforces_specialization_flag
                clr_country_flag = tank_specialization_flag
                set_country_flag = navy_specialization_flag
                clr_country_flag = air_specialization_flag
                mark_focus_tree_layout_dirty = yes
            }
		}
	}

    allies_air_specialization_preview = {
		icon = GFX_decision_generic_merge_plant_aircraft

		visible = { 
            is_in_faction_with = ENG
            has_global_flag = allies_high_command_flag
            OR = {
                tag = AST
                tag = ENG
                tag = CAN
                tag = SAF
                tag = FRA
                tag = BRA
            }
            NOT = {ROOT = {has_country_flag = specialization_chosen_flag}}
            ROOT = {has_country_flag = preview_flag}
        }
        
        available = { }

        priority = 100


		remove_effect = {
			ROOT = 
            {
                clr_country_flag = massmob_specialization_flag
                clr_country_flag = spforces_specialization_flag
                clr_country_flag = tank_specialization_flag
                clr_country_flag = navy_specialization_flag
                set_country_flag = air_specialization_flag
                mark_focus_tree_layout_dirty = yes
            }
		}
    }


    #########

    allies_end_preview = {
		icon = GFX_decision_generic_decision

		visible = { 
            is_in_faction_with = ENG
            has_global_flag = allies_high_command_flag
            OR = {
                tag = AST
                tag = ENG
                tag = CAN
                tag = SAF
                tag = FRA
                tag = BRA
            }
            NOT = {ROOT = {has_country_flag = specialization_chosen_flag}}
            ROOT = {has_country_flag = preview_flag}
        }
        
        available = { }

        priority = 100


		remove_effect = {
			ROOT = 
            {
                clr_country_flag = massmob_specialization_flag
                clr_country_flag = spforces_specialization_flag
                clr_country_flag = tank_specialization_flag
                clr_country_flag = navy_specialization_flag
                clr_country_flag = air_specialization_flag
                clr_country_flag = preview_flag
                mark_focus_tree_layout_dirty = yes
            }
		}
    }
}